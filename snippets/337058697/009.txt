o = det.raw
mask = o._mask(**kwa)      # returns cached combined mask, **kwa are not used, propagated from the Detector initialization.
mask = o._mask_comb(status=True, neighbors=False, edges=False, center=False, calib=False, umask=None, dtype=DTYPE_MASK, **kwa) # returns non-cached combined mask as specified in **kwa arguments
mask = o._mask_default(dtype=DTYPE_MASK, **kwa)
mask = o._mask_calib() # user defined mask saved in the calib DB for type mask_calib
mask = o._mask_calib_or_default(dtype=DTYPE_MASK, **kwa) # if available returns mask from "pixel_mask" calibration type, otherwise array of ones.
mask = o._mask_from_status(status_bits=0xffff, stextra_bits:(1<<64)-1, gain_range_inds=(0,1,2,3,4), dtype=DTYPE_MASK, **kwa)
mask = o._mask_edges(width=0, edge_rows=1, edge_cols=1, dtype=DTYPE_MASK, **kwa)
mask = o._mask_center(wcenter=0, center_rows=1, center_cols=1, dtype=DTYPE_MASK, **kwa)
mask = o._mask_neighbors(mask, rad=9, ptrn='r', **kwa)